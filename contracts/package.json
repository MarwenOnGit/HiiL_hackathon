{
  "name": "msme-contracts",
  "version": "0.1.0",
  "private": true,
  "description": "MSMEContractRegistry — anchors agreement fingerprints, tracks per-tier consent, verifies tamper-evidence. See ../ARCHITECTURE.md Section 7.",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "node": "hardhat node",
    "deploy:local": "hardhat run scripts/deploy.js --network localhost"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.22.0"
  }
}
